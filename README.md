```
pip install -r requirements.txt
```

or

```
pip install -r requirements.txt --upgrade --force-reinstall
```

usage

```
python cli.py -f ".\example\marguerite duras on bresson.mkv"
```

known issues

gpu device option does not work on my machine. windows path thing. not looking into it personally, don't need that functionality right now